# Configuración del servidor
server.port=8080
spring.application.name=centro-terapia-infantil

# Configuración de Base de Datos Supabase (Pooler IPv4 - Session Mode)
spring.datasource.url=jdbc:postgresql://aws-1-us-west-1.pooler.supabase.com:5432/postgres?sslmode=require
spring.datasource.username=postgres.nbkgkjyydseprspatyja
spring.datasource.password=Pa$$220.p99LL
spring.datasource.driverClassName=org.postgresql.Driver

# Configuración de JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.jdbc.lob.non_contextual_creation=true

# Consola H2 (Deshabilitada)
spring.h2.console.enabled=false

# Configuración de CORS
spring.web.cors.allowed-origins=http://localhost:5173
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*

# Configuración de Email (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=tu-email@gmail.com
spring.mail.password=tu-app-password
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuración de Uploads
app.upload.dir=${user.home}/centro-terapia/uploads
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Seguridad JWT
jwt.secret=MiSuperSecretKeyParaJWTQueDebeSerMuyLargaYSegura123456789
jwt.expiration=86400000
